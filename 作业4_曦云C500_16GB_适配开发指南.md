# 作业4：曦云 C500 / 16GB 适配开发验证指南

> 目标平台：沐曦 曦云C500 (XCORE 1.0架构) / 16GB HBM2e显存
> 软件栈：MXMACA 3.1 (兼容CUDA 11.x/12.x)
> 适用Agent：Trae AI Coding Agent
> 项目仓库：https://github.com/daimuguawang/llaisys-26s

---

## 一、环境准备与验证

### 1.1 MXMACA环境检查

曦云C500使用沐曦自研的MXMACA软件栈，提供CUDA兼容层。执行以下命令验证环境：

```bash
# 验证驱动（沐曦使用mx-smi替代nvidia-smi）
mx-smi
# 预期输出：显示曦云C500，驱动版本 >= 3.1.0，显存16GB

# 验证MXMACA工具链
mxcc --version
# 或使用hipcc（MXMACA兼容HIP）
hipcc --version
# 预期输出：MXMACA Compiler 3.1.0 或更高

# 验证环境变量
echo $MXMACA_PATH
echo $HIP_PATH
# 通常为 /opt/mx/maca 或 /opt/rocm（兼容模式）

# 验证设备可见性
ls /dev/mx*  # 或 /dev/kfd, /dev/dri
```

**曦云C500关键参数：**
- 架构：XCORE 1.0 (沐曦自研，兼容gfx906/gfx908类似目标)
- 显存：16GB HBM2e
- 显存带宽：~1 TB/s
- 支持数据类型：FP32, FP16, BF16, INT8 (TF32需软件模拟)
- 软件栈：MXMACA，兼容CUDA API，可直接编译大部分CUDA代码

### 1.2 兼容性说明

MXMACA对CUDA的兼容性：
- ✅ 大部分CUDA Runtime API (cudaMalloc, cudaMemcpy, cudaFree等)
- ✅ 基础CUDA C++语法 (__global__, __shared__, threadIdx等)
- ✅ cuBLAS兼容库 (通过rocBLAS/mxBLAS)
- ⚠️ 部分最新CUDA特性可能不支持（如FP8、Thread Block Clusters）
- ⚠️ 部分warp级原语需要适配
- ❌ cuDNN需使用沐曦版本或替代实现

### 1.3 项目初始化

```bash
# 克隆仓库
git clone https://github.com/daimuguawang/llaisys-26s.git
cd llaisys-26s

# 验证CPU基线
xmake f -cv
xmake
xmake install
pip install ./python/ -e

python test/test_runtime.py --device cpu
```

---

## 二、CUDA Runtime API 实现（MXMACA适配）

### 2.1 目录结构创建

```bash
# 为沐曦平台创建设备目录（复用nvidia命名空间，因为API兼容）
mkdir -p src/device/nvidia  # 保持代码兼容性，MXMACA编译时自动重定向

# 算子目录
for op in add argmax embedding linear rms_norm rope self_attention swiglu; do
    mkdir -p src/ops/$op/nvidia
done
```

### 2.2 Xmake编译配置（MXMACA版）

**步骤1：创建 `xmake/nvidia.lua`（适配MXMACA）**

```lua
-- xmake/nvidia.lua for 曦云C500 / MXMACA
-- MXMACA使用hipcc作为编译器，兼容CUDA代码

option("mx-gpu")
    set_default(false)
    set_showmenu(true)
    set_description("Enable MetaX C500 GPU support via MXMACA")
option_end()

target("llaisys_cuda")
    set_kind("static")
    
    -- MXMACA编译：使用hipcc，源文件后缀.cu会被自动处理
    if has_config("mx-gpu") then
        add_files("src/device/nvidia/*.cu")
        add_files("src/device/nvidia/*.cpp")
        
        -- 设置编译器为hipcc/mxcc
        set_toolchains("hip")  -- xmake hip工具链
        
        -- 目标架构（C500对应gfx908-like目标）
        add_cxxflags("-offload-arch=gfx908", {force = true})
        add_cuflags("-offload-arch=gfx908", {force = true})
        
        -- MXMACA头文件和库路径
        local maca_path = os.getenv("MXMACA_PATH") or "/opt/mx/maca"
        add_includedirs(maca_path .. "/include", {public = true})
        add_linkdirs(maca_path .. "/lib")
    else
        -- 标准NVIDIA CUDA配置
        add_files("src/device/nvidia/*.cu")
        add_files("src/device/nvidia/*.cpp")
        add_cugencodes("sm_120")
    end
    
    add_cxxflags("-O3", "-std=c++17", "-w")  # -w关闭警告，C500编译器可能有警告
    add_includedirs("include", "src", {public = true})
    
    -- 链接库（MXMACA兼容层）
    if has_config("mx-gpu") then
        add_links("amdhip64", "rocblas")  -- MXMACA兼容hip和rocblas
    else
        add_links("cudart", "cublas", "cublasLt")
    end
target_end()

-- 算子编译
for _, op in ipairs({"add", "argmax", "embedding", "linear", "rms_norm", "rope", "self_attention", "swiglu"}) do
target("op_" .. op .. "_cuda")
    set_kind("static")
    add_files("src/ops/" .. op .. "/nvidia/*.cu")
    add_deps("llaisys_cuda")
    if has_config("mx-gpu") then
        set_toolchains("hip")
        add_cxxflags("-offload-arch=gfx908", {force = true})
    else
        add_cugencodes("sm_120")
    end
    add_includedirs("include", "src", {public = true})
target_end()
end
```

**步骤2：修改根目录 `xmake.lua`**

```lua
option("nv-gpu")
    set_default(false)
    set_showmenu(true)
    set_description("Enable NVIDIA CUDA GPU support")
option_end()

option("mx-gpu")
    set_default(false)
    set_showmenu(true)
    set_description("Enable MetaX C500 GPU support")
option_end()

if has_config("nv-gpu") or has_config("mx-gpu") then
    add_defines("ENABLE_NVIDIA_API")  -- 复用相同宏，代码兼容
    includes("xmake/nvidia.lua")
end
```

### 2.3 Runtime API实现（兼容层适配）

**文件：`src/device/nvidia/nvidia_runtime_api.cu`**

> 注意：MXMACA的hipify工具会自动将cuda*函数转换为hip*函数，但我们可以直接编写兼容代码：

```cpp
#include "src/device/runtime_api.hpp"

// ========== MXMACA/CUDA兼容性处理 ==========
#ifdef __HIP_PLATFORM_AMD__  // MXMACA定义此宏
    #include <hip/hip_runtime.h>
    #include <rocblas/rocblas.h>
    // 别名兼容
    #define cudaMalloc hipMalloc
    #define cudaFree hipFree
    #define cudaMallocHost hipMallocHost
    #define cudaFreeHost hipFreeHost
    #define cudaMemcpy hipMemcpy
    #define cudaMemcpyHostToDevice hipMemcpyHostToDevice
    #define cudaMemcpyDeviceToHost hipMemcpyDeviceToHost
    #define cudaMemcpyDeviceToDevice hipMemcpyDeviceToDevice
    #define cudaSetDevice hipSetDevice
    #define cudaGetDevice hipGetDevice
    #define cudaGetDeviceCount hipGetDeviceCount
    #define cudaDeviceSynchronize hipDeviceSynchronize
    #define cudaStream_t hipStream_t
    #define cudaStreamCreate hipStreamCreate
    #define cudaStreamDestroy hipStreamDestroy
    #define cublasHandle_t rocblas_handle
    #define cublasCreate rocblas_create_handle
    #define cublasDestroy rocblas_destroy_handle
    #define cublasSetStream rocblas_set_stream
    // half类型兼容
    #define __half half
#else
    #include <cuda_runtime.h>
    #include <cublas_v2.h>
#endif

namespace nvidia {

// ========== 内存管理 ==========

static void* device_malloc(size_t size) {
    void* ptr = nullptr;
    auto err = cudaMalloc(&ptr, size);
    if (err != 0) return nullptr;  // 16GB显存注意检查分配失败
    return ptr;
}

static void device_free(void* ptr) {
    if (ptr) cudaFree(ptr);
}

static void* host_malloc(size_t size) {
    void* ptr = nullptr;
    cudaMallocHost(&ptr, size);
    return ptr;
}

static void host_free(void* ptr) {
    if (ptr) cudaFreeHost(ptr);
}

// ========== 内存拷贝 ==========

static void memcpy_h2d(void* dst, const void* src, size_t size) {
    cudaMemcpy(dst, src, size, cudaMemcpyHostToDevice);
}

static void memcpy_d2h(void* dst, const void* src, size_t size) {
    cudaMemcpy(dst, src, size, cudaMemcpyDeviceToHost);
}

static void memcpy_d2d(void* dst, const void* src, size_t size) {
    cudaMemcpy(dst, src, size, cudaMemcpyDeviceToDevice);
}

// ========== 设备管理 ==========

static void set_device(int device_id) {
    cudaSetDevice(device_id);
}

static int get_device() {
    int dev = 0;
    cudaGetDevice(&dev);
    return dev;
}

static int get_device_count() {
    int count = 0;
    cudaGetDeviceCount(&count);
    return count;
}

static void synchronize() {
    cudaDeviceSynchronize();
}

// ========== 资源管理 ==========

struct NvidiaResource {
    cublasHandle_t blas_handle;
    cudaStream_t stream;
};

static NvidiaResource* create_resource() {
    auto* res = new NvidiaResource();
    cublasCreate(&res->blas_handle);
    cudaStreamCreate(&res->stream);
    cublasSetStream(res->blas_handle, res->stream);
    return res;
}

static void destroy_resource(void* resource) {
    auto* res = static_cast<NvidiaResource*>(resource);
    cublasDestroy(res->blas_handle);
    cudaStreamDestroy(res->stream);
    delete res;
}

// ========== API表 ==========

static LlaisysRuntimeAPI cuda_api = {
    .device_malloc = device_malloc,
    .device_free = device_free,
    .host_malloc = host_malloc,
    .host_free = host_free,
    .memcpy_h2d = memcpy_h2d,
    .memcpy_d2h = memcpy_d2h,
    .memcpy_d2d = memcpy_d2d,
    .set_device = set_device,
    .get_device = get_device,
    .get_device_count = get_device_count,
    .synchronize = synchronize,
    .create_resource = create_resource,
    .destroy_resource = destroy_resource,
};

const LlaisysRuntimeAPI* getRuntimeAPI() {
    return &cuda_api;
}

}  // namespace nvidia
```

### 2.4 Runtime测试验证

```bash
# 配置编译（曦云C500模式）
xmake f --mx-gpu=y -cv

# 编译（C500编译较慢，耐心等待）
xmake -j$(nproc)

# 安装
xmake install

# 运行Runtime测试
python test/test_runtime.py --device nvidia

# 预期输出：
# test_malloc_free ... PASSED
# test_memcpy ... PASSED
# test_device_switch ... PASSED
```

**C500常见问题：**
- `hipErrorNoBinaryForGpu`: 确认`-offload-arch=gfx908`参数正确
- 内存分配失败：16GB显存有限，检查是否有泄漏
- 编译错误：C500编译器对C++模板支持较弱，遇到错误简化模板代码

---

## 三、CUDA算子实现（C500适配要点）

### 3.1 C500 Kernel编写注意事项

1. **避免使用CUDA 12新特性**：C500兼容CUDA 11.x为主
2. **Warp大小**：C500的warp大小为64（不是32），使用warpSize内置变量，不要硬编码32
3. **不支持FP8**：仅使用FP32/FP16/BF16
4. **Shared Memory**：每个compute unit约64KB，不要使用过大shared memory
5. **避免原子操作**：C500原子操作性能较差，尽量用规约替代

### 3.2 各算子C500适配版

#### 3.2.1 Add算子（兼容版）
```cuda
// 注意不要硬编码warpSize=32
__global__ void add_kernel(float* out, const float* a, const float* b, size_t n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    int stride = blockDim.x * gridDim.x;
    for (; idx < n; idx += stride) {  // grid-stride loop更兼容
        out[idx] = a[idx] + b[idx];
    }
}
```

#### 3.2.2 Linear（使用rocBLAS）
```cpp
// C500使用rocBLAS替代cublas，API基本一致
void linear_nvidia(tensor_t out, tensor_t in, tensor_t weight, tensor_t bias) {
    auto* res = static_cast<NvidiaResource*>(get_current_resource());
    auto handle = res->blas_handle;
    
    int M = in->shape()[0];
    int K = in->shape()[1];
    int N = weight->shape()[0];
    
    float alpha = 1.0f, beta = 0.0f;
    
#ifdef __HIP_PLATFORM_AMD__
    // rocBLAS版本
    rocblas_sgemm(handle,
        rocblas_operation_transpose, rocblas_operation_none,
        N, M, K,
        &alpha,
        (const float*)weight->data(), K,
        (const float*)in->data(), K,
        &beta,
        (float*)out->data(), N);
#else
    // cuBLAS版本
    cublasSgemm(handle,
        CUBLAS_OP_T, CUBLAS_OP_N,
        N, M, K,
        &alpha,
        (const float*)weight->data(), K,
        (const float*)in->data(), K,
        &beta,
        (float*)out->data(), N);
#endif

    // FP16版本：C500不支持tensor core GEMM，建议使用FP32计算
    // 或使用rocblas_hgemm
}
```

> **重要提示**：16GB显存有限，建议Linear层优先使用FP16存储权重，但计算用FP32保证精度。

#### 3.2.3 RMS Norm（C500优化版）
```cuda
// C500每个block建议使用256线程，不要超过512
__global__ void rms_norm_kernel(
    float* out, const float* in, const float* weight,
    int rows, int cols, float eps) {
    int row = blockIdx.x;
    int tid = threadIdx.x;
    int block_size = blockDim.x;
    
    __shared__ float s_sum[1];  // 小shared memory更兼容
    if (tid == 0) s_sum[0] = 0.0f;
    __syncthreads();
    
    float sum = 0.0f;
    for (int j = tid; j < cols; j += block_size) {
        float x = in[row * cols + j];
        sum += x * x;
    }
    
    // 规约：不要使用__shfl_down_sync，C500支持但mask参数不同
    // 使用shared memory规约更兼容
    __shared__ float warp_sum[64];  // warpSize=64 on C500
    int lane = tid % warpSize;
    int wid = tid / warpSize;
    
    for (int offset = warpSize/2; offset > 0; offset >>= 1) {
        sum += __shfl_down(sum, offset);  // C500支持简化版
    }
    if (lane == 0) warp_sum[wid] = sum;
    __syncthreads();
    
    if (tid < block_size / warpSize) {
        sum = warp_sum[tid];
        for (int offset = warpSize/2; offset > 0; offset >>= 1)
            sum += __shfl_down(sum, offset);
        if (tid == 0) atomicAdd(&s_sum[0], sum);
    }
    __syncthreads();
    
    float rms = rsqrtf(s_sum[0] / cols + eps);
    for (int j = tid; j < cols; j += block_size) {
        out[row * cols + j] = in[row * cols + j] * rms * weight[j];
    }
}
```

#### 3.2.4 Self-Attention（16GB显存优化版）
> **关键**：16GB显存有限，避免实例化完整的attention矩阵 [seq_len, seq_len]

```cpp
// 不要使用O(n^2)显存的实现！
// 1. 使用分块计算（tiled attention）
// 2. 或使用Flash Attention风格的在线softmax
// 3. KV Cache使用FP16

// 推荐：逐token计算attention，避免完整S矩阵
__global__ void self_attention_kernel(
    float* attn_out, const float* q, const float* k, const float* v,
    int seq_len, int n_head, int d, float scale) {
    // 每个block处理一个query位置
    int s = blockIdx.x;
    int h = blockIdx.y;
    int tid = threadIdx.x;
    
    // 在线softmax：不需要存储完整attention矩阵
    float max_score = -INFINITY;
    float sum_exp = 0.0f;
    float out_vec[64];  // d<=64时寄存器存储
    for (int i = 0; i < d; i++) out_vec[i] = 0.0f;
    
    for (int t = 0; t <= s; t++) {  // causal mask
        // 计算Q[s] · K[t]
        float score = 0.0f;
        for (int j = tid; j < d; j += blockDim.x) {
            score += q[(s*n_head+h)*d + j] * k[(t*n_head+h)*d + j];
        }
        // warp reduce score...
        score *= scale;
        
        // 在线softmax更新
        float new_max = fmaxf(max_score, score);
        float exp_old = expf(max_score - new_max);
        float exp_new = expf(score - new_max);
        sum_exp = sum_exp * exp_old + exp_new;
        
        // 更新输出
        for (int j = tid; j < d; j += blockDim.x) {
            out_vec[j] = out_vec[j] * exp_old + v[(t*n_head+h)*d + j] * exp_new;
        }
        max_score = new_max;
    }
    
    // 归一化并写回
    for (int j = tid; j < d; j += blockDim.x) {
        attn_out[(s*n_head+h)*d + j] = out_vec[j] / sum_exp;
    }
}
```

#### 3.2.5 其他算子
- **Argmax/Embedding/SwiGLU/ROPE**：这些是逐元素或简单规约kernel，基本不需要修改，注意grid-stride loop和不要硬编码warpSize=32即可。

### 3.3 算子测试

```bash
# 重新编译
xmake -j$(nproc)
xmake install

# 逐个测试算子
python test/ops/add.py --device nvidia
python test/ops/embedding.py --device nvidia
python test/ops/linear.py --device nvidia
python test/ops/rms_norm.py --device nvidia
python test/ops/rope.py --device nvidia
python test/ops/self_attention.py --device nvidia
python test/ops/swiglu.py --device nvidia
python test/ops/argmax.py --device nvidia

# 全部算子测试
python test/test_ops.py --device nvidia
```

---

## 四、16GB显存专项优化

### 4.1 显存预算（1.5B模型）

| 组件 | FP16显存占用 | 说明 |
|------|-------------|------|
| 模型权重 | ~3GB | 1.5B * 2 bytes |
| KV Cache (seq=2048) | ~1GB | 2 * layers * seq * dim * 2bytes |
| 激活值 | ~512MB | 推理时batch=1很小 |
| CUDA上下文 | ~512MB | 驱动开销 |
| 工作空间 | ~512MB | rocBLAS工作空间 |
| **总计** | **~5.5GB** | 16GB完全够用 |

> 如果运行更大模型，可启用：
> 1. 权重INT8量化（~1.5GB）
> 2. Paged Attention减少KV碎片
> 3. 激活重计算

### 4.2 KV Cache优化

```cpp
// KV Cache使用Paged Attention风格分配
// 1. 预分配大内存池
// 2. 按页分配，避免碎片
// 3. 16GB可支持最大seq_len=8192 (batch=1)
```

### 4.3 内存碎片处理

```cpp
// 在Runtime API中添加内存池
static void* device_malloc(size_t size) {
    // 简单内存池：对小分配(<1MB)使用池化
    if (size < 1024*1024) {
        return pool_alloc(size);
    }
    void* ptr = nullptr;
    hipMalloc(&ptr, size);
    return ptr;
}
```

---

## 五、端到端推理验证

### 5.1 推理测试

```bash
# 确保模型已下载
# huggingface-cli download deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B --local-dir ./models/Qwen1.5B

# 运行C500推理测试
python test/test_infer.py --model ./models/Qwen1.5B --test --device nvidia

# 显存监控（运行时另开终端）
watch -n 1 mx-smi
```

### 5.2 正确性验证标准

1. **数值一致性**：与CPU版本输出对比，最大误差 < 1e-2 (FP16) / 1e-4 (FP32)
   - C500浮点精度略低于NVIDIA，容差适当放宽
2. **生成文本匹配**：argmax采样下前50个token应与PyTorch一致
3. **性能指标（1.5B模型）**：
   - 预填充速度：> 100 tokens/s (batch=1, seq=128)
   - 解码速度：> 25 tokens/s (batch=1)
   - 显存峰值：< 8GB (seq=2048)

### 5.3 性能调优建议

1. **Batch Size=1时**：
   - 不要用cublas处理小矩阵，手写simple GEMM kernel可能更快
   - 算子融合：RMSNorm+Linear、SwiGLU+Linear融合
2. **使用FP16推理**：
   - 权重FP16，KV Cache FP16
   - 中间计算可保留FP32
3. **Stream并行**：
   - 数据传输和计算overlap

---

## 六、曦云C500常见坑点与解决方案

| 错误现象 | 原因 | 解决方案 |
|---------|------|---------|
| `hipErrorNoBinaryForGpu` | 架构不匹配 | 确认`-offload-arch=gfx908`，C500不支持gfx90a |
| 编译报错`__shfl_down_sync`未定义 | C500不支持完整mask参数 | 使用`__shfl_down`不带mask，或用shared memory规约 |
| 结果随机出错 | 缺少__syncthreads() | C500内存模型较松，确保共享内存同步 |
| rocBLAS GEMM结果错误 | leading dimension不对 | 仔细核对lda/ldb/ldc，行列主序与cublas一致 |
| 显存OOM但mx-smi显示有空余 | 内存碎片 | 启用内存池或预先分配大张量 |
| 运行hang住 | kernel死循环 | 检查grid-stride loop的边界条件 |
| `half`类型运算错误 | C500 half支持有限 | 关键计算转FP32，只在存储时用FP16 |
| xmake找不到hipcc | 工具链未配置 | `xmake f --mx-gpu=y --hipcc=/opt/mx/maca/bin/hipcc` |

---

## 七、验收Checklist

- [ ] `xmake f --mx-gpu=y && xmake && xmake install` 编译无错误
- [ ] `mx-smi`能正确识别设备
- [ ] `python test/test_runtime.py --device nvidia` 全部通过
- [ ] `python test/test_ops.py --device nvidia` 全部7个算子测试通过
- [ ] 算子支持FP32和FP16（BF16可选）
- [ ] `python test/test_infer.py --model [model_path] --test --device nvidia` 推理结果正确
- [ ] 显存峰值 < 10GB（16GB卡安全阈值）
- [ ] 解码速度 > 20 tokens/s
- [ ] 连续运行1000token无内存泄漏（mx-smi显存不持续增长）

---

## 八、调试技巧

1. **打印调试**：
   ```cpp
   // kernel中不能直接printf，先把数据拷回CPU打印
   hipMemcpy(host_data, device_data, size, hipMemcpyDeviceToHost);
   for (int i = 0; i < 10; i++) printf("%f ", host_data[i]);
   ```

2. **错误检查宏**：
   ```cpp
   #define HIP_CHECK(err) do { \
       if (err != 0) { \
           fprintf(stderr, "HIP error %d at %s:%d\n", err, __FILE__, __LINE__); \
           exit(1); \
       } \
   } while(0)
   ```

3. **与CPU逐算子对比**：
   ```bash
   # 先跑CPU版本保存中间结果，再对比GPU版本
   python test/ops/linear.py --device cpu --dump
   python test/ops/linear.py --device nvidia --compare
   ```
