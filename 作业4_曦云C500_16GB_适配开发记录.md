# 作业4：曦云 C500 / 16GB MXMACA 适配开发记录

> 目标平台：沐曦 曦云 C500 / 16GB HBM2e 显存
> 软件栈：MXMACA 3.2.1（兼容 CUDA 11.x/12.x）
> 模型：DeepSeek-R1-Distill-Qwen-1.5B
> 开发时间：2026-08-05 ~ 2026-08-06

---

## 一、环境概述

### 1.1 硬件配置

| 项目 | 规格 |
|------|------|
| GPU | 曦云 C500（XCORE 1.0 架构） |
| 显存 | 16GB HBM2e |
| Warp Size | 64（NVIDIA 为 32） |
| 支持数据类型 | FP32, FP16, BF16, INT8 |

### 1.2 软件栈

| 组件 | 版本/路径 |
|------|-----------|
| MXMACA | 3.2.1.10 |
| MXMACA 安装路径 | `/opt/maca-3.2.1` |
| 编译器 | `clang++-15`（位于 `/usr/bin/clang++-15`） |
| mxcc 脚本 | `/opt/maca-3.2.1/mxgpu_llvm/bin/mxcc`（Python 脚本，有语法错误无法直接使用） |
| MCBLAS 库 | `/opt/maca-3.2.1/lib/libmcblas.so` |
| 运行时头文件 | `/opt/maca-3.2.1/include/mcr/mc_runtime.h` |
| xmake | `/root/.local/bin/xmake`（需 `XMAKE_ROOT=y`） |

### 1.3 远程容器访问

通过 SSH 脚本 [scripts/ssh_compute_c500.sh](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/scripts/ssh_compute_c500.sh) 访问远程算力容器，支持 `login`、`exec`、`scp-up`、`scp-down` 等命令。

---

## 二、适配开发过程

### 2.1 总体策略

由于 MXMACA 的 `mxcc` 编译器脚本存在语法错误（Python f-string 未闭合），且直接使用 `clang++-15 -x cuda` 编译 CUDA 代码时会出现找不到 `libdevice`、目标 triple 不识别等问题，无法直接编译包含 `__global__` kernel 的 `.cu` 文件。

因此采用 **Host-Side 混合实现策略**：
- 将 `.cu` 文件重命名为 `.cpp`，以普通 C++ 代码编译
- 算子计算在主机端完成，通过 `mcMemcpy` 在设备内存和主机内存之间拷贝数据
- `linear` 算子的 GEMM 部分使用 MCBLAS 在设备端完成

### 2.2 编译配置

#### 2.2.1 xmake.lua 配置

在 [xmake.lua](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/xmake.lua) 中添加 `mx-gpu` 选项和 MXMACA 专用编译目标：

```lua
option("mx-gpu")
    set_default(false)
    set_showmenu(true)
    set_description("Whether to compile implementations for MetaX C500 GPU (MXMACA)")
option_end()

if has_config("mx-gpu") then
    add_defines("ENABLE_NVIDIA_API")
    add_defines("USE_MXMACA")
end
```

MXMACA 专用目标使用 `.mx_build/` 目录中的 `.cpp` 文件（从 `.cu` 重命名而来），避免 xmake 的 CUDA 规则检测。

#### 2.2.2 .mx_build 目录准备

```bash
rm -rf .mx_build
cp -r src .mx_build/src
find .mx_build/src -name "*.cu" -exec sh -c 'mv "$0" "${0%.cu}.cpp"' {} \;
```

#### 2.2.3 编译命令

```bash
export XMAKE_ROOT=y
export MXMACA_PATH=/opt/maca-3.2.1
xmake f --mx-gpu=y -cv
xmake -j4
xmake install
```

### 2.3 CUDA 兼容层（cuda_compat.hpp）

创建 [src/device/nvidia/cuda_compat.hpp](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/device/nvidia/cuda_compat.hpp)，将 CUDA API 映射到 MXMACA 的 `mc_*` API：

```cpp
#ifdef USE_MXMACA

#include <mcr/mc_runtime.h>
#include <mcr/mc_runtime_types.h>
#include <mcblas/mcblas.h>

// 类型映射
using half = mcblas_half;
using __nv_bfloat16 = mcblas_bfloat16;
using cudaMemcpyKind = mcMemcpyKind;
using cudaDataType_t = macaDataType_t;

// 函数映射
#define cudaMalloc mcMalloc
#define cudaFree mcFree
#define cudaMemcpy mcMemcpy
#define cudaDeviceSynchronize mcDeviceSynchronize
// ... 更多映射

// Warp Size
#define WARP_SIZE 64

// Warp shuffle 兼容
#define __shfl_down_sync(mask, val, offset) __shfl_down(val, offset)

// Host-Side 工具函数
template <typename T>
std::vector<T> mxm_copy_to_host(const T* dev_ptr, size_t count);

template <typename T>
void mxm_copy_to_device(T* dev_ptr, const T* host_ptr, size_t count);

#endif
```

### 2.4 算子适配

8 个算子均采用 Host-Side 混合实现模式：

1. `cudaDeviceSynchronize()` 确保设备计算完成
2. `mxm_copy_to_host()` 将设备数据拷贝到主机
3. 在主机端执行计算
4. `mxm_copy_to_device()` 将结果拷贝回设备

| 算子 | 实现方式 | 特殊处理 |
|------|---------|----------|
| add | Host-Side 逐元素加法 | 支持 F32/F16/BF16 |
| argmax | Host-Side 扫描求最大值 | 结果通过 `mxm_copy_to_device` 写回 |
| embedding | Host-Side 查表拷贝 | 先拷贝 index 到主机，再查找 weight 行 |
| linear | MCBLAS GEMM + Host bias | GEMM 在设备端，bias 加法在主机端 |
| rms_norm | Host-Side 均方根归一化 | 逐行计算 |
| rope | Host-Side 三角函数计算 | 逐位置逐头计算 |
| self_attention | Host-Side 软注意力 | 含 causal mask 和 softmax |
| swiglu | Host-Side 逐元素激活 | SiLU 激活函数 |

### 2.5 测试框架适配

#### 2.5.1 test_utils.py 修改

修改 [test/test_utils.py](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/test/test_utils.py)，使 PyTorch 参考数据在 CPU 上生成：

```python
def random_tensor(shape, dtype_name, device_name, ...):
    # MXMACA 兼容：PyTorch 数据在 CPU 上生成
    torch_device_name = "cpu" if device_name == "nvidia" else device_name
    torch_tensor = torch.rand(shape, dtype=..., device=torch_device(torch_device_name))
    
    # 使用 H2D 拷贝
    memcpy_kind = llaisys.MemcpyKind.H2D if device_name == "nvidia" else llaisys.MemcpyKind.D2D
    api.memcpy_sync(llaisys_tensor.data_ptr(), torch_tensor.data_ptr(), bytes_, memcpy_kind)
```

`check_equal` 函数也相应修改，使用 D2H 拷贝将 llaisys 结果取回 CPU 比较。

#### 2.5.2 test_infer.py 修改

- PyTorch 模型在 CPU 上加载（因为 C500 不支持 PyTorch CUDA）
- 添加 `--skip-hf` 选项跳过慢速的 PyTorch CPU 参考推理

#### 2.5.3 qwen2.py 修改

修改 [python/llaisys/models/qwen2.py](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/python/llaisys/models/qwen2.py)，使用 `safetensors.torch` 替代 `safetensors.numpy` 读取模型权重，解决 numpy 不支持 bfloat16 的问题：

```python
import safetensors.torch
import torch

# 使用 torch 加载 safetensors，直接获取原始字节
state_dict = safetensors.torch.load_file(str(file))
raw_tensor = tensor_data.cpu().contiguous()
ptr_raw = raw_tensor.data_ptr()
byte_size = raw_tensor.numel() * raw_tensor.element_size()
data_bytes = (ctypes.c_ubyte * byte_size)()
ctypes.memmove(data_bytes, ptr_raw, byte_size)
```

---

## 三、踩坑记录

### 3.1 mxcc 编译器脚本损坏

**现象**：`/opt/maca-3.2.1/mxgpu_llvm/bin/mxcc` 是一个 Python 脚本，但存在语法错误（f-string 未闭合），无法执行。

**解决**：绕过 mxcc，直接使用 `clang++-15` 编译，并手动添加 MXMACA 的 include 和 lib 路径。

### 3.2 CUDA 头文件找不到

**现象**：编译时报 `cuda_runtime.h` not found。

**原因**：MXMACA 不提供 `cuda_runtime.h`，而是使用 `mcr/mc_runtime.h`。

**解决**：在 `cuda_compat.hpp` 中通过 `#ifdef USE_MXMACA` 条件编译，包含 MXMACA 的头文件并映射所有 CUDA 类型和函数。

### 3.3 stdint.h 类型未定义

**现象**：编译时报 `uint8_t`、`uint32_t` 等类型未定义。

**原因**：MXMACA 的 `mc_runtime_types.h` 使用了 `uint8_t` 等类型，但在 C 模式下未自动包含 `<stdint.h>`。

**解决**：在包含 MXMACA 头文件前先包含 `<cstdint>` 或 `<stdint.h>`。

### 3.4 Warp Size 差异

**现象**：规约算子结果不正确。

**原因**：C500 的 warp size 为 64，而 CUDA 代码中硬编码了 32。

**解决**：定义 `WARP_SIZE` 宏为 64，并调整规约循环。但由于最终采用 Host-Side 实现，此问题不再影响计算结果。

### 3.5 `__shfl_down_sync` 不兼容

**现象**：编译报 `__shfl_down_sync` 未定义。

**原因**：MXMACA 的 `__shfl_down` 不支持 mask 参数。

**解决**：添加宏 `#define __shfl_down_sync(mask, val, offset) __shfl_down(val, offset)`。

### 3.6 严格别名违规

**现象**：编译报 `dereferencing type-punned pointer will break strict-aliasing rules`。

**原因**：MXMACA 的 `maca_bfloat16.hpp` 中存在类型双关代码。

**解决**：在 xmake.lua 中添加 `-fno-strict-aliasing` 编译选项。

### 3.7 Device-Side 变量未定义

**现象**：编译报 `blockIdx`、`threadIdx`、`blockDim` 未声明。

**原因**：`.cu` 文件被重命名为 `.cpp` 后，编译器将其视为普通 C++ 代码，不识别 CUDA 的内置变量。

**解决**：采用 Host-Side 混合实现策略，所有计算在主机端完成，避免使用 device-side 变量。

### 3.8 cudaMemcpyKind 类型未定义

**现象**：编译报 `cudaMemcpyKind` does not name a type。

**解决**：在 `cuda_compat.hpp` 中添加 `using cudaMemcpyKind = mcMemcpyKind;`。

### 3.9 MCBLAS 数据类型映射错误

**现象**：编译报 `cudaDataType_t` was not declared、`MCBLAS_R_32F` was not declared。

**原因**：MXMACA 使用 `macaDataType_t` 和 `MACA_R_*` 常量，而非 CUDA 的 `cudaDataType_t` 和 `CUDA_R_*`。

**解决**：
```cpp
using cudaDataType_t = macaDataType_t;
#define CUDA_R_32F MACA_R_32F
#define CUDA_R_16F MACA_R_16F
#define CUDA_R_16BF MACA_R_16BF
```

### 3.10 argmax 设备指针直接赋值

**现象**：argmax 测试失败。

**原因**：MXMACA 的设备指针不能在主机端直接解引用赋值（`*max_idx = local_idx`）。

**解决**：使用 `mxm_copy_to_device(max_idx, &local_idx, 1)` 替代直接赋值。

### 3.11 embedding 主机访问设备指针

**现象**：embedding 测试失败。

**原因**：在主机端直接遍历设备指针 `index[r]` 读取设备内存。

**解决**：先将 `index` 拷贝到主机缓冲区 `h_index`，再在主机端遍历。

### 3.12 numpy 不支持 bfloat16

**现象**：模型加载时报 `TypeError: data type 'bfloat16' not understood`。

**原因**：容器中 numpy 1.26.4 不支持 `np.bfloat16` 类型。

**解决**：将 `qwen2.py` 中的权重加载从 `safetensors.numpy` 改为 `safetensors.torch`，使用 PyTorch 读取 bfloat16 张量，再通过 `data_ptr()` 和 `ctypes.memmove` 获取原始字节。

### 3.13 PyTorch 无法使用 C500 GPU

**现象**：PyTorch 模型加载时尝试使用 CUDA 但失败。

**原因**：PyTorch 不支持 MXMACA 的 GPU 设备。

**解决**：
- `test_infer.py`：PyTorch 模型在 CPU 上加载
- `test_utils.py`：PyTorch 参考数据在 CPU 上生成
- 添加 `--skip-hf` 选项跳过慢速的 PyTorch CPU 参考推理

### 3.14 xmake CUDA 检测干扰

**现象**：xmake 尝试检测 CUDA SDK 但失败。

**原因**：MXMACA 不是标准 CUDA 安装，xmake 的 CUDA 规则无法识别。

**解决**：创建 `.mx_build/` 目录，将 `.cu` 文件重命名为 `.cpp`，以普通 C++ 代码编译，完全绕过 xmake 的 CUDA 规则检测。

### 3.15 头文件路径问题

**现象**：编译报 `swiglu_nvidia.hpp: No such file or directory`。

**原因**：`.mx_build/` 目录中的文件结构需要与 `src/` 完全一致，包括所有头文件。

**解决**：使用 `cp -r src .mx_build/src` 完整复制整个 src 目录。

---

## 四、最终验证结果

### 4.1 测试结果汇总

| 测试项 | 状态 | 说明 |
|--------|------|------|
| Runtime 测试 | ✅ 通过 | 发现 1 个 nvidia 设备 |
| add 算子 | ✅ 通过 | F32/F16/BF16 |
| argmax 算子 | ✅ 通过 | F32/F16/BF16 |
| embedding 算子 | ✅ 通过 | F32/F16/BF16 |
| linear 算子 | ✅ 通过 | F32/F16/BF16 |
| rms_norm 算子 | ✅ 通过 | F32/F16/BF16 |
| rope 算子 | ✅ 通过 | F32/F16/BF16 |
| self_attention 算子 | ✅ 通过 | F32/F16/BF16 |
| swiglu 算子 | ✅ 通过 | F32/F16/BF16 |
| 推理测试（16 tokens） | ✅ 通过 | Token 完全匹配 PyTorch |
| 推理测试（128 tokens） | ✅ 通过 | 输出完整文本 |

### 4.2 性能数据

| 指标 | 数值 |
|------|------|
| 模型 | DeepSeek-R1-Distill-Qwen-1.5B |
| 推理 tokens | 128 |
| llaisys C500 推理时间 | 9.29s |
| 解码速度 | ~13.8 tokens/s |
| PyTorch CPU 推理时间（16 tokens） | 74.35s |
| llaisys C500 推理时间（16 tokens） | 3.50s |
| 加速比（vs CPU） | 约 8x |

### 4.3 推理输出示例

```
Prompt: Who are you?

Output: Greetings! I'm DeepSeek-R1, an artificial intelligence assistant 
created by DeepSeek. I'm at your service and would be delighted to assist 
you with any inquiries or tasks you may have.
```

---

## 五、修改的文件清单

| 文件 | 修改内容 |
|------|---------|
| [scripts/ssh_compute_c500.sh](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/scripts/ssh_compute_c500.sh) | 新建，SSH 访问脚本 |
| [xmake.lua](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/xmake.lua) | 添加 mx-gpu 选项和 MXMACA 编译目标 |
| [src/device/nvidia/cuda_compat.hpp](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/device/nvidia/cuda_compat.hpp) | 新建，CUDA/MXMACA 兼容层 |
| [src/ops/add/nvidia/add_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/add/nvidia/add_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [src/ops/argmax/nvidia/argmax_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/argmax/nvidia/argmax_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [src/ops/embedding/nvidia/embedding_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/embedding/nvidia/embedding_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [src/ops/linear/nvidia/linear_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/linear/nvidia/linear_nvidia.cu) | 添加 MCBLAS GEMM + Host bias |
| [src/ops/rms_norm/nvidia/rms_norm_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/rms_norm/nvidia/rms_norm_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [src/ops/rope/nvidia/rope_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/rope/nvidia/rope_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [src/ops/self_attention/nvidia/self_attention_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/self_attention/nvidia/self_attention_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [src/ops/swiglu/nvidia/swiglu_nvidia.cu](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/src/ops/swiglu/nvidia/swiglu_nvidia.cu) | 添加 MXMACA Host-Side 实现 |
| [python/llaisys/models/qwen2.py](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/python/llaisys/models/qwen2.py) | 使用 safetensors.torch 加载权重 |
| [test/test_utils.py](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/test/test_utils.py) | PyTorch 数据在 CPU 上生成 |
| [test/test_infer.py](file:///Users/daimuguawang/我的文件/program/llaisys-26s_test/llaisys-26s/test/test_infer.py) | 添加 --skip-hf 选项 |

---

## 六、后续优化方向

1. **Native Device Kernel**：将简单逐元素算子（add/swiglu/rms_norm）改为原生 MXMACA device kernel，消除 D2H/H2D 数据搬运开销
2. **linear bias device kernel**：在设备端添加 bias 加法 kernel，避免每层 GEMM 后的 D2H/H2D 往返
3. **self_attention 在线 softmax**：使用 Flash Attention 风格的在线 softmax，减少中间存储
4. **性能目标**：解码速度提升至 > 20 tokens/s
